<Page
    x:Class="Divar_UWP.Views.CitySelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" FlowDirection="RightToLeft" Background="{ThemeResource DivarPageBrush}">
    <Grid Padding="18,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="{Binding Title}" Style="{StaticResource DivarPageTitleStyle}" />
            <TextBlock Margin="0,5,0,14" Text="شهر خود را برای نمایش آگهی‌های همان منطقه انتخاب کنید." Style="{StaticResource DivarBodyStyle}" />
            <TextBox x:Name="CitySearchBox" Style="{StaticResource DivarTextBoxStyle}" PlaceholderText="جستجو در شهرهای دیوار" TextChanged="OnSearchTextChanged" />
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,14,0,10" Orientation="Horizontal" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressRing Width="22" Height="22" IsActive="{Binding IsLoading}" Foreground="{StaticResource DivarAccentBrush}" />
            <TextBlock Margin="10,0,0,0" Text="در حال دریافت شهرها…" Style="{StaticResource DivarBodyStyle}" VerticalAlignment="Center" />
        </StackPanel>
        <Border Grid.Row="2" Margin="0,4,0,10" Padding="14" Background="{StaticResource DivarAccentTintBrush}" Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource DivarBodyStyle}" />
                <Button Margin="0,10,0,0" Content="تلاش دوباره" HorizontalAlignment="Right" Style="{StaticResource DivarSecondaryButtonStyle}" Click="OnRetryClick" />
            </StackPanel>
        </Border>
        <ListView Grid.Row="3" ItemsSource="{Binding Cities}" IsItemClickEnabled="True" ItemClick="OnCityItemClick" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid MinHeight="50" Padding="8,4">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Foreground="{ThemeResource DivarTextBrush}" FontSize="16" VerticalAlignment="Center" />
                            <TextBlock Margin="8,0,0,0" Text="پربازدید" Foreground="{StaticResource DivarAccentBrush}" FontSize="11" VerticalAlignment="Center" Visibility="{Binding IsTopCity, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </StackPanel>
                        <FontIcon Grid.Column="1" Glyph="&#xE76C;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="{ThemeResource DivarSubtleTextBrush}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
