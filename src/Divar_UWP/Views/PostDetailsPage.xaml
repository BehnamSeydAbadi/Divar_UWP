<Page
    x:Class="Divar_UWP.Views.PostDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FlowDirection="RightToLeft"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <StackPanel MaxWidth="480" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Margin="0,12,0,24" Text="نمای بومی جزئیات آگهی آمادهٔ اتصال به سرویس پست است." TextWrapping="Wrap" />
            <Button Content="بازگشت" Command="{Binding BackCommand}" HorizontalAlignment="Stretch" />
        </StackPanel>
    </Grid>
</Page>
